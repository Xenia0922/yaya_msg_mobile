# 牙牙消息 · UI 全套重构方案（2026-08-15）

> 目标：从「静态入口目录」升级为「内容驱动的现代移动应用」。现有 iOS 26 设计 token
> 保留并扩展，布局逻辑整体重建。

---

## 1. 现状诊断

| 页面/组件 | 现状 | 问题 |
|:--|:--|:--|
| HomeScreen | 7 组「标题 + 2 列玻璃卡片网格」，全部是静态导航入口 | 与 v2.6.4 及更早的「分组卡片网格」**布局逻辑相同**，只是换皮；首页无任何真实内容 |
| ScreenHeader | 粉色发光标题 + 居中三栏 | 旧视觉语言（霓虹粉 + textShadow），与新版设计系统冲突 |
| Media / Rooms / Music / Messages / Settings | 旧样式：`isDark && xDark` 双分支、白卡片、直角列表 | 未接入 Palette 设计系统 |
| 底部导航 | 玻璃胶囊 4 tab | 保留（已是新版） |
| 设计系统 | colors/typography/spacing/radii/shadows/motion + GlassCard/Pill/Button/ListItem | token 齐备但只被 Home 使用 |

## 2. 设计原则

1. **内容优先**：首页展示真实数据（直播、最近播放），导航入口降级为次要层级。
2. **信息分层**：大标题 → 横向内容带 → 快捷操作 → 折叠工具区，从「目录页」变「仪表盘」。
3. **统一语言**：所有页面走 Palette 双主题 token；卡片用 GlassCard；标题用 typography 梯度；
   不再出现 `isDark && xDark` 硬编码分支。
4. **克制视觉**：去掉霓虹发光（textShadow），用玻璃描边 + 语义色；动效统一 Spring/短时长。

## 3. 新信息架构（首页）

```
┌──────────────────────────────────────┐
│ 牙牙消息 · largeTitle                 │  问候区（成员数/登录态）
│ 下午好 · 已收录 317 位成员             │
├──────────────────────────────────────┤
│ 正在直播                   全部 ›      │  ① 直播横向轮播（真实数据，
│ ┌─────┐ ┌─────┐ ┌─────┐             │     pocketApi.getLiveList）
│ │封面 │ │封面 │ │封面 │              │     封面卡 + 成员名 + 标题
│ └─────┘ └─────┘ └─────┘             │
├──────────────────────────────────────┤
│ 快捷入口（横向胶囊 chips，可滚动）      │  ② 高频操作：直播/回放/B站/音乐/私信/翻牌
│ [🎥 直播][📺 回放][📡 B站][🎵 音乐]... │
├──────────────────────────────────────┤
│ 最近播放（音乐 store 有记录时显示）     │  ③ 内容续接：歌曲名/成员 → 点按续播
│ ┌──────────────────────────────────┐ │
│ │ ▶ 歌名 — 成员         继续播放 ›  │ │
│ └──────────────────────────────────┘ │
├──────────────────────────────────────┤
│ 工具（手风琴分组，默认展开最近一组）    │  ④ 低频入口折叠收纳：
│ ▸ 直播（直播/回放/上麦/B站）           │     组标题可折叠，每组 2 列紧凑入口
│ ▸ 口袋（房间/私信/相册/公演）          │
│ ▸ 翻牌 / 成员 / 资源 / 工具            │
└──────────────────────────────────────┘
```

## 4. 页面布局范式

| 范式 | 适用 | 实现 |
|:--|:--|:--|
| 仪表盘 | Home | 大标题 + 内容轮播 + chips + 折叠区 |
| 大标题列表页 | Media（直播/回放）、Music、Messages、私信 | ScreenHeader 新样式 + 卡片列表项 |
| 分组设置列表 | Settings、账号、数据库 | inset 分组（圆角组卡片 + 分隔线） |
| 内容网格 | 音乐库 | 2 列封面卡片（CoverArt + 渐变兜底） |

## 5. 组件升级清单

1. **ScreenHeader（重写）**：左对齐大标题（title3 起）、玻璃圆形返回键（MaterialCommunityIcons
   chevron-left）、右侧操作位保留；去粉色发光；`overlay` 属性兼容图片背景页。
2. **HomeScreen（重写）**：见第 3 节；数据失败静默降级（不阻塞启动）。
3. **MusicLibraryScreen（重写列表项）**：2 列封面卡（CoverArt 已有）+ 歌名 + 成员 + 时长。
4. **SettingsScreen（重写）**：inset 分组列表（GlassCard 分组 + ListItem 行）。
5. **MessagesScreen / MediaScreen（列表项升级）**：ListItem 统一行样式，去 isDark 分支。

## 6. 实施顺序与验证

1. ScreenHeader → 全站内容页头部立即统一
2. HomeScreen 仪表盘 → 首页布局质变
3. 音乐/设置/消息列表页
4. Media 列表项
5. typecheck → assembleRelease → MuMu 安装 → uiautomator/截图回归

## 7. 风险与兼容

- ScreenHeader props 保持向后兼容（title/onBack/right/style），全站零改调用点。
- Home 数据拉取失败静默降级（现有 try/catch 模式）。
- 不触碰导航结构（Tab/Stack 路由不变），只改布局与视觉。
