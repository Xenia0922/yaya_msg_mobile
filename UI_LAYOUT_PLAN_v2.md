# 牙牙消息 · 布局分析与改版规划 v2（2026-08-15）

> 方法：先诊断现状布局 → 定设计目标 → 给出新布局线框 → 说明布局依据。
> 本规划覆盖 首页 / 直播 / 房间 / 录播 四个核心页面，用户确认后按序实施。

---

## 0. 设计总则（所有页面的布局依据）

1. **内容优先**：屏幕第一眼必须是内容（直播/消息），不是入口和控件。
2. **控件收敛**：单屏顶部控件不超过 1 行（筛选/搜索合并，不用第二行堆控件）。
3. **层级清晰**：大标题 → 内容 → 次级入口，纵向信息流自然滚动。
4. **密度匹配**：列表页用 2 列网格（封面内容密度高），详情/消息用全宽。
5. **统一范式**：封面一律「全幅 + 渐变遮罩 + 白字上浮」；筛选一律横向滚动 chips；卡片圆角 16。

---

## 1. 首页 · 现状诊断与新方案

### 现状问题
```
大标题「牙牙消息」+ 问候 + 日期
[正在直播 横向轮播]            ← 内容只占 15% 屏高
[快捷入口 3×2 网格]            ← 6 个入口，与工具区重复
[最近播放]                     ← 有播放才有
[工具手风琴 6 组展开一组]       ← 占屏 60%，本质还是入口目录页
```
诊断：**结构上仍是「入口目录」**，直播内容被压缩到 15%，用户首屏看不到多少内容。

### 新方案
```
大标题「牙牙消息」+ 问候（1 行，去掉日期行）
┌────────────────────────────────┐
│ [正在直播]        全部 >        │
│ ┌──────────────────────────┐   │
│ │ 全宽沉浸卡：直播封面(16:9) │   │  ← 第一条直播全宽大卡
│ │ 渐变+白字标题+成员+直播中   │   │     (2 秒滑动切换下一条)
│ └──────────────────────────┘   │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐   │
│ │ 2 列网格：其余直播小卡        │   ← 封面+白字标题
│ └────┘ └────┘ └────┘ └────┘   │
│ [最近播放]（有播放时单行条）     │
│ [快捷入口] 单行 4 个（直播/回放/ │
│  音乐/私信）                    │
│ [工具] 单行横向 chips（翻牌/     │
│  统计/下载/账号/设置…）          │
└────────────────────────────────┘
```
布局依据：首屏内容占比从 15% → 65%；入口从"3×2 网格 + 6 组手风琴"收敛为"4 快捷 + 一行 chips"；直播是第一高频操作，占最大版面。

---

## 2. 直播页 · 现状诊断与新方案

### 现状问题
```
ScreenHeader「直播 · 回放」
[直播][回放] pill  + [刷新]      ← 控件行 1
[全部][SNH][BEJ][GNZ][CKG][CGT]  ← 控件行 2
[搜索框] [日历]                  ← 控件行 3
全宽沉浸式封面卡列表（208dp 高）   ← 每屏 2.5 条，密度低
```
诊断：**顶部三行控件堆积**，内容区被压到 55% 屏高；全宽卡密度低；直播/录播共用同一列表结构，无区分度。

### 新方案
```
ScreenHeader「直播」+ 右侧 [录播 >] 入口
[全部|SNH|BEJ|GNZ|CKG|CGT]  ← 单行滚动 chips（含搜索图标，点击展开搜索条）
┌────────────────────────────────┐
│ [精选直播] 全宽 banner（16:9）   │  ← 第一条直播大图轮播
│  渐变+白字标题+在线感            │
├────────┬───────────┤
│ 网格卡  │ 网格卡     │          ← 其余直播 2 列网格
│ 封面+   │ 封面+      │             (封面+白字标题+直播中红标)
│ 白字    │ 白字       │
├────────┴───────────┤
│ 加载更多 / 到底        │
└────────────────────┘
```
布局依据：三行控件收敛为一行；内容密度翻倍（2 列网格）；"直播中"红标给临场感；录播独立入口不再混在 pill 里。

---

## 3. 录播页 · 现状诊断与新方案

### 现状问题
```
与直播共用同一列表（全宽卡）或独立后仍是 2 列网格，但缺日期维度：
录播核心诉求 = 「按日期/成员找历史回放」，当前只有连续列表，无时间分组。
```

### 新方案
```
ScreenHeader「录播」
[搜索框]（成员/标题）
[日期分段 chips：今天|昨天|更早|日历]
┌───────┬────────┐
│ 网格卡 │ 网格卡  │   ← 2 列网格：封面+时长角标+白字标题
├───────┼────────┤
│ …     │        │
└───────┴────────┘
（今天/昨天/更早 分组头，按月聚类）
```
布局依据：录播的本质是"回看"，时间维度是第一信息；网格密度 + 日期分组兼顾浏览与检索。

---

## 4. 房间页 · 现状诊断与新方案

### 现状问题
```
ScreenHeader「口袋房间」+ 刷新
副标题「关注房间、大房间和小房间消息」
[MemberPicker 搜索成员并打开房间]   ← 占一整行，控件感强
会话式列表（56 头像+名字+消息+TEAM·时间+关注按钮）
```
诊断：搜索条+副标题占顶部 25%；列表偏"工具感"（关注按钮/置顶 pill 抢眼），缺少"成员消息流"的亲切感。

### 新方案
```
大标题「房间」
[搜索图标]（点击展开搜索条，平时不占行）
┌──────────────────────────┐
│ 置顶成员横向条：圆形头像+名字  │  ← 有置顶时显示
└──────────────────────────┘
┌───────┬────────┐
│ 成员卡 │ 成员卡  │  ← 2 列卡片：72dp 圆头像+名字+最后消息
│        │        │     预览（2 行，淡化）
├───────┼────────┤
│ …     │        │
└───────┴────────┘
```
布局依据：搜索收敛为图标（点击展开）；2 列头像网格是粉丝追星类 app（Bubble/微博关注）的通用范式，一眼可扫全部关注成员；消息预览保留在卡片内保持信息量。

---

## 5. 实施顺序

| 步骤 | 页面 | 工作量 |
|---|---|---|
| 1 | 直播页（banner+网格+控件收敛） | 中 |
| 2 | 录播页（日期分组） | 中 |
| 3 | 房间页（头像网格） | 中 |
| 4 | 首页（内容优先重构） | 大 |

每步完成后：tsc → 构建 → MuMu 安装 → 像素/uiautomator 验证 → 提交。
